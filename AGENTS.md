# AGENTS.md

Codex 가 이 저장소에서 작업을 이어받기 위한 컨텍스트 문서.
**작업 시작 전 이 파일을 먼저 읽을 것.**

---

## 프로젝트

**Urban-MicroGrid** — 미기후 융합 지역 전력수요·피크 위험 예측
팀 **Watt the hell (2조)** / 코디세이 Term-Project 아이디어톤 / 도메인 AI Energy

**한 줄 정의**
법정동 단위로 전력수요를 예측하고, 동별 상대 임계를 넘으면 피크 경보를 내는 서비스.
공간(토지피복) 대리변수를 기온과 곱한 상호작용항으로 넣어 "동네마다 다른 냉방 반응"을 잡는다.

**중요: 구현 완성이 목표가 아니라 아이디어톤 발표가 목표다.**
데모로 보여줄 수 있는 최소 범위를 확실히 하고, 나머지는 "확장 단계"로 명시한다.

---

## 대상과 기간

```
구로구 구로동  : 법정동코드 1153010100
은평구 진관동  : 법정동코드 1138011400     ← 10800 은 다른 동. 절대 혼동 금지
분석 기간      : 2022-06-28 ~ 2022-10-14   (전력·S-DoT·ASOS 교집합)
여름 구간      : 2022-06-28 ~ 2022-08-31
```

> 초기에는 창신동이었으나 **구로동으로 교체**되었다.
> 문서·발표자료에 남아 있는 창신동 예비값(불투수 83.0% / 식생 13.1%)은
> **구로동 값이 아니므로 그대로 쓰면 안 된다.**

---

## 절대 어길 수 없는 규칙

이 규칙들은 코드에 강제되어 있다. 리팩터링할 때 깨뜨리지 말 것.

1. **비교는 항상 정규화 지표 R 로.** 절대 kWh 로 두 동을 비교하면
   미기후가 아니라 동네 크기를 측정하게 된다.
2. **예측 타깃은 실측 총전력(`kwh`).** 냉방 민감도는 예측 대상이 아니라
   모델의 해석 산출물(β)이다. 기온으로 만든 값을 기온으로 예측하면 순환논리.
3. **피크 임계는 동별 백분위.** 절대 kWh 임계는 큰 동을 항상 위험으로 만든다.
4. **랜덤 K-fold 금지.** 시계열 누수다. `models.time_split` 만 쓴다.
5. **측정하지 않은 성능 수치를 만들지 않는다.** 개선율은 실제 두 모델을
   돌린 결과에서만 산출한다.
6. **공간 연산은 EPSG:5186/5179 에서.** 경위도(4326)에서 면적을 재면 왜곡된다.
   4326 은 지도 표출 전용.
7. **프론트엔드는 계산하지 않는다.** 등급·색상·문구까지 백엔드가 만들어 보낸다.
8. **미확보 자료는 500 이 아니라 `pending` 으로 답한다.** null + status + note 를
   함께 보내면 프론트가 스켈레톤을 띄운다. 화면이 스스로 "확보 중"이라고 말한다.

---

## 데이터 함정 (전부 실제로 겪은 것)

| 함정 | 증상 | 방어 위치 |
|---|---|---|
| 진관동 법정동코드 | 10800 으로 뽑으면 값이 6천↔41만 으로 튐 | `config.DONGS` |
| 전력 CSV 완전중복 | 대상 동 추출 결과의 약 50% 가 중복행 | `io_loaders._to_hourly` |
| 스프레드시트 절단 | 행 수가 정확히 1,048,575 에서 끊김 | `load_power_csv` 경고 |
| 시각 표기 | 0100~2400 형식. 라벨 h 를 [h−1,h) 로 해석 | `hm_to_timestamp` |
| S-DoT 위치 부재 | 환경정보 CSV 에 위치 컬럼이 없음(모델명 단일값) | `load_sdot` 예외 |
| 도엽 ≠ 법정동 | 진관동 도엽은 북한산이 74.6% → ISR 5.4% 로 나옴 | `coverage_from_sheet` warning |
| centroid 가 동 밖 | 오목한 폴리곤 | `representative_point()` |

---

## 현재 상태

### 완료 (실측 결과 확보)
- 전력 + ASOS 결합, 기저수요 B, 정규화 R, 전환온도 T\*, 민감도 β, 피크 임계 θ
- 대응표본 t-검정
- 프론트 응답 JSON 직렬화
- 토지피복 SHP/DBF 순수 파이썬 파싱 (의존성 없이 동작)
- **불투수·식생 피복률 산출** (도엽 10장, 생활권 100m 버퍼) → `docs/06`
- **FastAPI 백엔드** (`urban_microgrid/api/`) — 7개 엔드포인트, 계약 회귀 테스트
  대시보드 목업의 화면 하나에 API 하나가 대응한다. 화면에 없는 API 는 만들지 않는다
  (경보 목록은 목업에 패널이 없어 제거. `serialize.build_alerts` 는 남아 있다)
- **지도 폴리곤** `/api/dongs/geojson` — 경계 GeoJSON 이 있으면 채움색까지 실어 보낸다
- **AI 브리핑** `/api/briefing` — Gemini. 모델은 분석하지 않고 실측 사실표를 문장으로만 옮긴다.
  `briefing.verify()` 가 사실표에 없는 숫자를 검출한다 (규칙 5 의 코드 강제)

### 막혀 있음 (외부 자료 대기)
| 항목 | 필요 자료 | 신청처 |
|---|---|---|
| ΔT, 야간 열섬, Ablation | **S-DoT 설치위치 매핑** | http://iothub.eseoul.go.kr/publicData/insertForm.do |
| ISR/VCR **확정값** (현재는 생활권 버퍼 예비값) | **법정동 경계 SHP** | 서울 데이터 허브 / 국가공간정보포털 |
| 지도 공백 메우기 | 도엽 **37608037 · 37608097** | EGIS 자료신청 |
| 상호작용 β₃ | 분석 동 3개 이상 | OA-22835 동일 파일에서 추가 추출 |

---

## 재현된 예비 결과 (2022-06-28 ~ 08-31)

| 지역 | 평균 R | T\* | β | 야간 R | 주간 R | θ |
|---|---|---|---|---|---|---|
| 진관동 | 33.1% | 18.0℃ ⚠ | 2.77 %p/℃ | 31.2% | 34.7% | 35,945 kWh |
| 구로동 | 27.6% | 19.0℃ | 2.76 %p/℃ | 17.8% | 35.8% | 37,974 kWh |

일평균 R 차이(구로동 − 진관동) = **−5.37 %p** (95% CI [−8.65, −2.08], n=45일, p=0.0026, d=−0.48)

⚠ 진관동 T\* 는 탐색 격자 하한에 붙음 → 기간이 짧아 식별이 약함.

**해석 (발표에서 이 톤을 유지할 것)**
냉방 민감도 β 는 두 지역이 사실상 동일했고, 차이는 야간 수요 패턴에서 나타났다.
방향이 가설과 반대이며, **미기후보다 건물 용도·재실 패턴이 지배적**일 가능성이 크다.
→ 비교지역을 주거 우세 동으로 조정하고 생활인구를 통제변수로 추가해야 한다.

이 결과를 숨기지 않는다. "예상과 달랐고, 그래서 다음 단계가 필요하다"가 발표 전략이다.

### 토지피복 — 생활권 100m 버퍼 (도엽 10장, EPSG:5186) → `docs/06`

| 지표 | 구로동권 | 진관동권 |
|---|---|---|
| 불투수 ISR | **78.8%** | **31.2%** |
| 식생 VCR | **15.9%** | **57.4%** |
| 수면 WSR | 0.9% | 0.9% |
| 도시열 지수 | **81.5 (매우 높음)** | **36.9 (낮음)** |

두 지역 도시열 지수 차이 **44.6점**.

- 나지·습지는 불투수·식생 어디에도 넣지 않는다 → **세 비율 합은 100%가 아니다**
- **법정동 경계 클리핑 전** 값이다. `config.MICROCLIMATE_BASIS.clipped_to_dong = False`
  이 사실이 API 응답(`note`·`caveat`)을 타고 화면까지 간다
- 도엽 전체값(진관동 ISR 5.37 / VCR 87.59)은 북한산이 지배해 프로젝트 지표가 아니다

---

## 구조

```
urban_microgrid/
├── config.py       모든 경로·상수·수식 파라미터 (값 변경은 여기서만)
├── io_loaders.py   원자료 적재 — 형식 차이를 전부 흡수
├── features.py     피복률·MCI·ΔT·기저수요·정규화 R
├── models.py       전환온도·민감도·상호작용·예측
├── evaluate.py     성능지표·경보·통계검정
├── landcover.py    토지피복 GIS (순수 파이썬 + geopandas 두 경로)
├── serialize.py    프론트엔드 JSON 직렬화
├── briefing.py     AI 브리핑 사실표·프롬프트·숫자 검증
├── llm.py          LLM 호출부 (Gemini 어댑터)
├── run_demo.py     전체 파이프라인
└── api/            FastAPI 백엔드
    ├── schemas.py  계약을 pydantic 타입으로 고정
    ├── store.py    데이터 원천 (live: 원자료 / snapshot: 실측 산출물)
    ├── routes.py   엔드포인트 7개
    └── main.py     앱 조립 · 예외 → 상태코드

tests/test_api.py   계약 회귀 테스트 (발표 전 한 번 돌릴 것)

docs/               수식 정의서 · API 계약 · 발표 스크립트 · 데이터 인벤토리
data/               원자료 (git 제외)
out/                산출물 (git 제외)
```

---

## 실행

```bash
pip install -r requirements.txt

python -m urban_microgrid.run_demo                          # 분석 파이프라인
uvicorn urban_microgrid.api.main:app --reload --port 8000   # 백엔드 API
python tests/test_api.py                                    # 계약 회귀 테스트
```

원자료는 `data/` 에 넣는다(`UMG_DATA_DIR` 로 덮어쓸 수 있다).
GIS 클리핑을 하려면 `geopandas` 가 필요하다(선택 의존성).

**API 는 원자료가 없어도 뜬다.** 원자료가 있으면 `live` 모드로 파이프라인을 돌리고,
없으면 `snapshot` 모드로 `docs/api_sample/` 의 실측 산출물을 서빙한다.
지금 어느 쪽인지는 `GET /api/meta` 가 알려준다. 자세한 내용은
[docs/05_백엔드_실행가이드.md](docs/05_백엔드_실행가이드.md).

---

## 다음 작업 (우선순위)

1. **법정동 경계 SHP 확보 → `landcover.coverage_for_dong()` 실행**
   현재 값은 생활권 100m 버퍼 **예비값**이고 법정동 경계로 자른 값이 아니다.
   확정값이 나오면 `config.MICROCLIMATE_PRELIM` 을 갱신하고
   `MICROCLIMATE_BASIS.clipped_to_dong = True` 로 바꾼다 → 화면 문구가 자동으로 바뀐다.
2. **도엽 37608037 · 37608097 신청** — 지도 모서리 공백(수치에는 영향 없음).
3. **S-DoT 위치 매핑 확보 → `io_loaders.load_sdot()` 활성화**
   → ΔT 생성 → `run_demo` STEP 8(Ablation) 자동 활성화
4. **법정동 경계 → 지도 폴리곤**
   `python -m urban_microgrid.landcover 법정동경계.shp` → `data/dong_boundaries.geojson`
   서버 재시작하면 `/api/dongs/geojson` 이 `pending` → `ready` 로 바뀐다.
5. **버퍼 민감도 분석** `landcover.buffer_sensitivity()` 로 50/100/200m 비교,
   성능이 가장 좋은 반경 채택
6. **생활인구 결합** — 용도·재실 통제변수
7. **동 3개 이상 확장** → `models.fit_interaction()` 활성화 (β₃ 추정)

---

## 작업 시 주의

- 새 상수를 코드에 하드코딩하지 말고 `config.py` 에 넣는다.
- 등급 문구·색상은 `serialize.HEAT_GRADES` / `RISK_GRADES` 두 리스트에서만 관리한다.
- 결과 수치를 문서에 옮길 때는 `[프로젝트 예비값]` 태그를 붙인다.
- 인과 표현 금지: "A 때문에 B" ❌ → "A 와 같은 방향으로 B 가 관측됨" ✅
- 2개 동 결과에는 항상 "2개 동 사례, 일반화는 향후 과제" 를 병기한다.
