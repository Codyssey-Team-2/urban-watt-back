# 백엔드 API — 이 셋만 있으면 실측 스냅샷 모드로 서버가 뜬다
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# AI 브리핑 (선택) — 키가 없으면 API 가 pending 으로 답한다
google-genai>=1.0

# 분석 파이프라인 (live 모드 · run_demo)
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1

# 통계 (상호작용 모델)
statsmodels>=0.14

# GIS — 법정동 경계 클리핑·버퍼에 필요 (선택)
# landcover.coverage_from_sheet() 는 이것 없이도 동작한다
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
